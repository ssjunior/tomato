// import store from "store";

// import actions from "../store/reducers/actions";

// const page = (slice, url, key = "edit") => {
//   let nextAction = [actions.edit.updateFromFetch];
//   store.dispatch(actions.fetch.get(slice, url, nextAction, key));
// };

// const pages = (url, moduleId) => {
//   let nextAction = [actions.list.listUpdateState];
//   store.dispatch(actions.fetch.get(moduleId, url, nextAction));
// };

// let load = {
//   page: page,
//   pages: pages
// };

// export default load;
